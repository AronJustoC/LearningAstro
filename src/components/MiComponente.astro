---
//Definimos props con una interfaz
interface Props {
  title: string;
  counter: number;
}
//Tipamos las pros de astro 'Astro.props'
const { title, counter } = Astro.props;
---

<h2>{title}</h2>
<p>Este comentario se renderizo {counter} veces</p>
